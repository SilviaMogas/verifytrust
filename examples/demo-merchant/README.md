# NOVA GOODS — DEMO MERCHANT

Run `pnpm start` after installing the workspace dependencies. The script
creates a demo issuer, signs a credential for a NOVA Travel Bottle, and prints
the base64url credential plus the `issuerKeyHash` that the operator must
approve for `nova-goods` in `IssuerRegistry`.
